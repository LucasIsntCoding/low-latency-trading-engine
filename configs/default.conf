# Low-Latency Trading Engine Simulator configuration
symbol = SIM
seed = 42

# Simulated market-data generation
initial_mid_ticks = 10000      # 100.00 when PRICE_SCALE = 100
base_spread_ticks = 2
min_qty = 10
max_qty = 500
max_depth_levels = 12

# Strategy parameters
lookback = 64
threshold_ticks = 3
order_qty = 25
cooldown_events = 50

# Risk limits
max_abs_position = 500
max_order_qty = 100
max_gross_notional = 1000000

# Persistence and observability
book_sample_every = 1000
use_redis = false
redis_host = 127.0.0.1
redis_port = 6379
